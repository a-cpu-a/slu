
--Hello world!

mod prelude;

--todo-late-desugaring: hoist functions outside of functions?
safe extern "C" {
	fn puts(line = *:>::std::u8) -> :>::std::i32;
}
---Test test test !!!
ex fn _println(text = &:>::std::str) -> {}
{
	--puts(text);
}