#
# Dependencies: basic, type, exportPrefix, letStatement, functionStatement
#

New Syntax {
	
	stat @::= | OptExportPrefix ‘local’ uncondDestrPat [‘=’ explist]
}

New Keywords {
	local
}

Local Statement {
	
	Just like let, except the type are inferred as dynamic types sometimes, like "function" statements.
}