#
# Dependencies: basic, type
#

New Syntax {
	
	stat @::= | [label] "while" basicExp blockOrRet
}

New Keywords {
	while
}

While loop {
	
	Loop while a condition evaluates to a result with a value, not a error.
	The condition is checked before running the block.
	
	```slu
	while x<65
	{
		x = x+1;
		printX(x);
	}
	```
}
