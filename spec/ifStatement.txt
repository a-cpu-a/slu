#
# Dependencies: basic, type
#

New Syntax {
	
	--Note: retstat as used here, cant use the `else` operator, as it would conflict with if-stat parsing
	blockOrRet ::= retstat [‘;’] | ‘{’ block ‘}’

	ifcond ::= "if" exp
	
	stat @::= | ifcond "do" blockOrRet {"else" ifcond "do" blockOrRet} ["else" blockOrRet]
	stat @::= | ifcond "=>" exp
	exp @::= | ifcond "=>" exp
	basicExp @::= | ifcond "=>" basicExp
	binop @::= | "else"
}

New Keywords {
	if
	else
}

If Statement {
	
	Lets you check a bool value.
	
	The return type is assigned automatically, if missing.
	When in expressions, the return type is not decayed into a value, unless explicitly set as such, or if one of the cases would return a value.
	```slu
	--Both y and z are dropped, due to the possibility that x is either value.
	drop if x => y else z;
	
	--Since they dont decay unless needed, you can put the ref operator outside the if.
	let */y/z selected = &(if x => y else z)
	```
	
	```slu

	let x = if true
			  => 0
			else 1

	if true => 100
	else if true => 100
	else if true => {1,2,3}
	else if true => do {return 100;}
	else if true => print("Hello")
	else		do {print("Hello");}
	
	if true do{} else if true do{} else {}

	if true do
		return 1; 
	else if true
		return 2; 
	else 
		return 3;

	```
	
	Implementation {
		May be a jump table, if else chain, etc.
	}
}